import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  searchResults: {
    marginVertical: 30,
    marginHorizontal: 10,
    gap: 10,
    padding: 5,
  },
});
export default styles;
